# Third-Party Notices

EVIDIQ Bulwark MCP uses the following open-source packages:

- `@modelcontextprotocol/sdk` (MIT) — Model Context Protocol SDK
- `mcp-handler` (MIT) — Streamable HTTP transport for MCP
- `zod` (MIT) — TypeScript-first schema validation
- `viem` (MIT) — TypeScript interface for Ethereum (EIP-191 signing)
- `@okxweb3/x402-core`, `@okxweb3/x402-evm` — OKX x402 payment SDK (Phase 2)
- `@0gfoundation/0g-storage-ts-sdk` — 0G decentralized storage SDK (optional)
- `ethers` (MIT) — Ethereum library used by the 0G SDK (optional)

Development-only: `typescript`, `tsx`, `vitest`, `@types/node`.
